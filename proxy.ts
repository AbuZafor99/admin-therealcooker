export { auth as proxy } from "@/auth";
export const config = { matcher: ["/((?!api/auth|login|forgot-password|verify-email|reset-password|_next/static|_next/image|favicon.ico|moneykee-logo.png).*)"] };
